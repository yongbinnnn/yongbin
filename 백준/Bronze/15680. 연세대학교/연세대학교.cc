#include <stdio.h>

int main(void)
{
    int N;
    scanf("%d", &N);
    
    if(N == 1)
    {
        printf("Leading the Way to the Future");
    }
    else if(N == 0)
    {
        printf("YONSEI");
    }
    
    return 0;
}